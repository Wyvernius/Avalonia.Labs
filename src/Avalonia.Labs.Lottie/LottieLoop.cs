using System;
using System.Collections.Generic;
using System.Text;

namespace Avalonia.Labs.Lottie
{
    public enum AnimationLoop
    {
        None = 0,
        StartBegin = 1, // Start at begin of animation.
        StartLoop = 2 // Start at begin of loop.
    }
}
